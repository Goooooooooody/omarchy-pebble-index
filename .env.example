# Copy to ~/.config/omarchy-pebble-index/.env and chmod 0600.
# Start receiver / setup copies this file if .env is missing. Never commit a real key.
# Setup does not read OMP or other credential stores.

OPENROUTER_API_KEY=
